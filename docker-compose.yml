version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: kool
      MONGO_INITDB_ROOT_PASSWORD: master
    ports:
      - 27017:27017
